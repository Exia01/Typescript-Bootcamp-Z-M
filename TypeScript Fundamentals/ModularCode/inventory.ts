import ProductInterface from './product-interface';
export function checkInventory(product: ProductInterface): string {
    // Example logic for checking inventory
    return `Checking inventory for ${product.name}`;
};
